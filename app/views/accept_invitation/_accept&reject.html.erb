<%= form_with(:url => accept_invitation_index_path ) do |f| %>
  <%= f.hidden_field :user_id, value: user.id %>
  <%= f.submit "Accept Request" %>
<% end %>
<%= form_with(:url => accept_invitation_path) do |f| %>
  <%= f.hidden_field :user_id, value: user.id %>
  <%= f.submit "Decline Request" %>
<% end %>
